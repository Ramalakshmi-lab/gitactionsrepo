name: Hello World Workflow
on: [push]
jobs:
  hello_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1
      - name: Say Hello
        run: echo "Hello, World!"
